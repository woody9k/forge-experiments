# Shared experimental utilities

Deliberately empty. Code lands here only when multiple experiment plugins
need it and the platform does not — see the decision rules in the platform's
docs/platform-split-plan.md §5/§13. Do not pre-promote.
