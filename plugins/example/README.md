# Example plugin (pendulum-lab) — placeholder

A small, complete reference plugin demonstrating every Forge contribution
point (manifest, observations, selftests, task types, SAGE tools + pack, UI
section) on a deliberately simple domain — a damped pendulum with a
known-answer validation (period vs. the small-angle formula). Lands with the
conformance harness (platform backlog P-2).
